version: "3.8"

services:
  rag-app:
    build: .
    container_name: advanced-rag
    ports:
      - "8501:8501"
    env_file:
      - .env
    volumes:
      - ./.indices:/app/.indices
    restart: unless-stopped
